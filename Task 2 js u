ji1	Create a simple Login Screen with User Name, Password fields and Submit,Reset,Sign Up and Password Reset Button.
2	Once the User Clicks on Submit Button then Log the Username and password data in the Console.
3	Once the User Clicks on Reset Button then clear the Username and password fields in the UI.
4	Create a User Object and store the entered User Name in a key called userName and Password into pwd key inside the User Object.
5	Once the User Clicks on Submit Button then Log the Username and password JSON Data in the Console.
6	Add a place holder in the User Name box as Enter the User Name and in password box as Enter the Password.
7	If the user clicks on the username and moves out of the username box by clicking or on tab then run a validation to check if the field is empty and add a Red Color to the Border.
8	If the user clicks on the username and moves out of the Password box by clicking or on tab then run a validation to check if the field is empty and add a Red Color to the Border.
9	If the User Clicks on Submit Button without entering either the username or password then Show a Window Alert Message stating that username and password is mandatory.
10	If the User Clicks on Submit Button without entering either the username or password then place the cursor on the first empty Field, i.e if username is empty then focus should go to username field on clicking on Submit Button and if password is empty and username is not empty then only focus should go to password field.
11	Disable the Submit Button if Both Username and Password is empty.
12	Hide the Reset Button if either the Username and Password is empty.
13	Add a place holder in the User Name box as Enter the User Name and in password box as Enter the Password.
14	DO AFTER SIGN UP Page is Completed - Validate the UserName and Password if its present in the Local Storage on Submit Button and if the UserName is not present then throw an Boot Strap Error Message Stating  "User Name not Present"  and if username is present and Password is wrong then throw an error message stating that "Password Wrong Please try Again" 
 






please write a code in different file html css js < please don't miss any step also don't miss to add reset button and functionality
?



    <!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="styles.css">
  <script defer src="script.js"></script>
  <title>Login Page</title>
</head>
<body>
  <div class="container">
    <form id="loginForm">
      <label for="username">Username</label>
      <input type="text" id="username" placeholder="Enter the User Name" onblur="validateEmpty('username')" onfocus="removeValidation('username')">
      <label for="password">Password</label>
      <input type="password" id="password" placeholder="Enter the Password" onblur="validateEmpty('password')" onfocus="removeValidation('password')">
      <button type="button" id="submitBtn" onclick="submitForm()" disabled>Submit</button>
      <button type="button" id="resetBtn" onclick="resetForm()">Reset</button>
      <button type="button" id="signUpBtn" onclick="signUp()">Sign Up</button>
      <button type="button" id="passwordResetBtn" onclick="passwordReset()">Password Reset</button>
    </form>
  </div>
</body>
</html>

body {
  font-family: Arial, sans-serif;
  display: flex;
  align-items: center;
  justify-content: center;
  height: 100vh;
  margin: 0;
}

.container {
  width: 300px;
}

form {
  display: flex;
  flex-direction: column;
}

label {
  margin-bottom: 5px;
}

input {
  margin-bottom: 10px;
  padding: 5px;
}

button {
  padding: 8px;
  margin-top: 10px;
  cursor: pointer;
}

#resetBtn {
  display: none;
}

.validation {
  border: 2px solid red;
}


function submitForm() {
  const username = document.getElementById('username').value;
  const password = document.getElementById('password').value;

  if (!username || !password) {
    alert('Username and password are mandatory');

    if (!username) {
      document.getElementById('username').focus();
    } else {
      document.getElementById('password').focus();
    }

    return;
  }

  // Check local storage for user data
  const storedUserData = localStorage.getItem(username);

  if (!storedUserData) {
    alert('Username not present');
    return;
  }

  const storedUserObject = JSON.parse(storedUserData);

  // Validate username and password against local storage
  if (storedUserObject.pwd !== password) {
    alert('Password wrong. Please try again');
    return;
  }

  // Log the Username and password JSON Data in the Console
  console.log('Username and password JSON Data:', JSON.stringify(storedUserObject));
}

// Rest of the code remains

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            margin: 0;
            padding: 0;
            display: flex;
            align-items: center;
            justify-content: center;
            height: 100vh;
        }

        form {
            background-color: #fff;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            width: 300px;
            text-align: center;
        }

        input {
            width: 100%;
            padding: 8px;
            margin: 8px 0;
            box-sizing: border-box;
        }

        button {
            background-color: #4caf50;
            color: white;
            padding: 10px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
        }

        button:disabled {
            background-color: #cccccc;
            cursor: not-allowed;
        }

        .error {
            color: red;
        }
    </style>
</head>
<body>

<form id="loginForm">
    <label for="username">Username:</label>
    <input type="text" id="username" placeholder="Enter the User Name">

    <label for="password">Password:</label>
    <input type="password" id="password" placeholder="Enter the Password">

    <button type="button" id="submitBtn" onclick="submitForm()">Submit</button>
    <button type="button" id="resetBtn" onclick="resetForm()">Reset</button>
    <button type="button" id="signupBtn" onclick="signup()">Sign Up</button>
    <button type="button" id="resetPasswordBtn" onclick="resetPassword()">Password Reset</button>
</form>

<script>
    document.getElementById('username').addEventListener('blur', function () {
        validateField('username');
    });

    document.getElementById('password').addEventListener('blur', function () {
        validateField('password');
    });

    function validateField(field) {
        const element = document.getElementById(field);
        if (element.value.trim() === '') {
            element.style.border = '2px solid red';
        } else {
            element.style.border = '1px solid #ccc';
        }
    }

    function submitForm() {
        const username = document.getElementById('username').value;
        const password = document.getElementById('password').value;

        if (username === '' || password === '') {
            alert('Username and password are mandatory');
            return;
        }

        const userObject = {
            userName: username,
            pwd: password
        };

        console.log('Username and password data:', username, password);
        console.log('Username and password JSON data:', JSON.stringify(userObject));

        // Additional logic for local storage validation can be added here
    }

    function resetForm() {
        document.getElementById('username').value = '';
        document.getElementById('password').value = '';
    }

    function signup() {
        // Additional logic for sign up can be added here
    }

    function resetPassword() {
        // Additional logic for password reset can be added here
    </script>
</body>
</html>











new .....


<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            margin: 0;
            padding: 0;
            display: flex;
            align-items: center;
            justify-content: center;
            height: 100vh;
        }

        form {
            background-color: #fff;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            width: 300px;
            text-align: center;
        }

        input {
            width: 100%;
            padding: 8px;
            margin: 8px 0;
            box-sizing: border-box;
        }

        button {
            background-color: #4caf50;
            color: white;
            padding: 10px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
        }

        button:disabled {
            background-color: #cccccc;
            cursor: not-allowed;
        }

        .error {
            color: red;
        }
    </style>
    <title>Login Page</title>
</head>
<body>

<form id="loginForm">
    <label for="username">Username:</label>
    <input type="text" id="username" placeholder="Enter the User Name">

    <label for="password">Password:</label>
    <input type="password" id="password" placeholder="Enter the Password">

    <button type="button" id="submitBtn">Submit</button>
    <button type="button" id="resetBtn">Reset</button>
    <button type="button" id="signupBtn">Sign Up</button>
    <button type="button" id="resetPasswordBtn">Password Reset</button>

    <script>
        document.getElementById('username').addEventListener('blur', function () {
            validateField('username');
        });

        document.getElementById('password').addEventListener('blur', function () {
            validateField('password');
        });

        document.getElementById('submitBtn').addEventListener('click', function () {
            submitForm();
        });

        document.getElementById('resetBtn').addEventListener('click', function () {
            resetForm();
        });

        function validateField(field) {
            const element = document.getElementById(field);
            if (element.value.trim() === '') {
                element.style.border = '2px solid red';
            } else {
                element.style.border = '1px solid #ccc';
            }
        }

        function submitForm() {
            const username = document.getElementById('username').value;
            const password = document.getElementById('password').value;

            if (username === '' || password === '') {
                alert('Username and password are mandatory');
                if (username === '') {
                    document.getElementById('username').style.border = '2px solid red';
                    document.getElementById('username').focus();
                } else {
                    document.getElementById('username').style.border = '1px solid #ccc';
                    document.getElementById('password').style.border = '2px solid red';
                    document.getElementById('password').focus();
                }
                return;
            }

            const userObject = {
                userName: username,
                pwd: password
            };

            console.log('Username and password data:', username, password);
            console.log('Username and password JSON data:', JSON.stringify(userObject));

            // Additional logic for local storage validation can be added here
        }

        function resetForm() {
            document.getElementById('username').value = '';
            document.getElementById('password').value = '';
        }

        function signup() {
            // Additional logic for sign up can be added here
        }

        function resetPassword() {
            // Additional logic for password reset can be added here
        }
    </script>
</form>

</body>
</html>



new 2....

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            margin: 0;
            padding: 0;
            display: flex;
            align-items: center;
            justify-content: center;
            height: 100vh;
        }

        form {
            background-color: #fff;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            width: 300px;
            text-align: center;
        }

        input {
            width: 100%;
            padding: 8px;
            margin: 8px 0;
            box-sizing: border-box;
        }

        button {
            background-color: #4caf50;
            color: white;
            padding: 10px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
        }

        button:disabled {
            background-color: #cccccc;
            cursor: not-allowed;
        }

        .error {
            color: red;
        }
    </style>
    <title>Login Page</title>
</head>
<body>

<form id="loginForm">
    <label for="username">Username:</label>
    <input type="text" id="username" placeholder="Enter the User Name">

    <label for="password">Password:</label>
    <input type="password" id="password" placeholder="Enter the Password">

    <button type="button" id="submitBtn">Submit</button>
    <button type="button" id="resetBtn">Reset</button>
    <button type="button" id="signupBtn">Sign Up</button>
    <button type="button" id="resetPasswordBtn">Password Reset</button>

    <script>
        document.getElementById('username').addEventListener('blur', function () {
            validateField('username');
        });

        document.getElementById('password').addEventListener('blur', function () {
            validateField('password');
        });

        document.getElementById('submitBtn').addEventListener('click', function () {
            submitForm();
        });

        document.getElementById('resetBtn').addEventListener('click', function () {
            resetForm();
        });

        function validateField(field) {
            const element = document.getElementById(field);
            if (element.value.trim() === '') {
                element.style.border = '2px solid red';
            } else {
                element.style.border = '1px solid #ccc';
            }
        }

        function submitForm() {
            const username = document.getElementById('username').value;
            const password = document.getElementById('password').value;

            if (username === '' || password === '') {
                alert('Username and password are mandatory');
                if (username === '') {
                    document.getElementById('username').style.border = '2px solid red';
                    document.getElementById('username').focus();
                } else {
                    document.getElementById('username').style.border = '1px solid #ccc';
                    document.getElementById('password').style.border = '2px solid red';
                    document.getElementById('password').focus();
                }
                return;
            }

            const userObject = {
                userName: username,
                pwd: password
            };

            console.log('Username and password data:', username, password);
            console.log('Username and password JSON data:', JSON.stringify(userObject));

            // Additional logic for local storage validation can be added here
        }

        function resetForm() {
            const usernameField = document.getElementById('username');
            const passwordField = document.getElementById('password');

            usernameField.value = '';
            passwordField.value = '';

            if (usernameField.value === '' && passwordField.value === '') {
                document.getElementById('resetBtn').style.display = 'none';
            } else {
                document.getElementById('resetBtn').style.display = 'inline-block';
            }
        }

        function signup() {
            // Additional logic for sign up can be added here
        }

        function resetPassword() {
            // Additional logic for password reset can be added here
        }
    </script>
</form>

</body>
</html>



